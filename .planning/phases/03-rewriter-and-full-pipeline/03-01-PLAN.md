---
phase: 03-rewriter-and-full-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ai/rewriter.js
  - src/ai/pipeline.js
  - src/ai/pipeline.test.js
  - src/config.js
  - package.json
autonomous: true
requirements:
  - PIPELINE-01
  - PIPELINE-02
  - PIPELINE-04
  - MODELS-01
must_haves:
  truths:
    - "src/ai/rewriter.js exists and exports rewriteDraft as an async function (stub returning null)"
    - "src/ai/pipeline.js exists and exports runPipeline as an async function (stub returning {lyrics, tags, title})"
    - "src/ai/pipeline.test.js exists with 6 test cases that FAIL (RED) against the stubs"
    - "config.ai.rewriterModel is defined and defaults to 'google/gemini-2.5-flash'"
    - "package.json test script invokes metrics.test.js, critic.test.js, and pipeline.test.js"
    - "package.json check script includes rewriter.js and pipeline.js"
    - "npm run check passes (all files parse cleanly)"
    - "Wave 1 pipeline tests FAIL (RED state) — confirming the test contract is real"
  artifacts:
    - path: "src/ai/rewriter.js"
      provides: "async stub: rewriteDraft(lyrics, critique) returns null"
      exports: ["rewriteDraft"]
      contains: "google/gemini-2.5-flash"
    - path: "src/ai/pipeline.js"
      provides: "async stub: runPipeline(input) calls generateLyrics and returns {lyrics, tags, title}"
      exports: ["runPipeline"]
      contains: "runPipeline"
    - path: "src/ai/pipeline.test.js"
      provides: "6 integration tests (RED) covering PIPELINE-01/02/04, MODELS-01"
      contains: "GENERIC_DRAFT"
    - path: "src/config.js"
      provides: "config.ai.rewriterModel field"
      contains: "rewriterModel"
    - path: "package.json"
      provides: "test:pipeline script and updated check/test scripts"
      contains: "test:pipeline"
  key_links:
    - from: "src/ai/pipeline.test.js"
      to: "src/ai/pipeline.js"
      via: "import { runPipeline } from './pipeline.js'"
      pattern: "from '\\./pipeline\\.js'"
    - from: "src/ai/pipeline.js"
      to: "src/ai/rewriter.js"
      via: "import { rewriteDraft } from './rewriter.js'"
      pattern: "from '\\./rewriter\\.js'"
    - from: "src/ai/rewriter.js"
      to: "src/config.js"
      via: "import { config } from '../config.js'"
      pattern: "from '\\.\\./config\\.js'"
---

<objective>
Create the Phase 3 skeleton — stubs for rewriter.js and pipeline.js, the test contract (RED
state), config extension, and npm script wiring. After this plan completes, all 6 tests in
pipeline.test.js MUST run and FAIL because the stubs return trivial defaults (null / passthrough).

Wave 2 replaces rewriter.js stub with real implementation.
Wave 3 replaces pipeline.js stub with real orchestrator and wires the bot.

Purpose: Lock the contract (function signatures, imports, test expectations) before writing
any OpenRouter calls. No network calls happen in this wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rewriter-and-full-pipeline/03-RESEARCH.md
@.planning/phases/03-rewriter-and-full-pipeline/03-PATTERNS.md
@.planning/phases/03-rewriter-and-full-pipeline/03-VALIDATION.md
@src/ai/critic.js
@src/ai/critic.test.js
@src/ai/metrics.js
@src/config.js
@package.json
</context>

<interfaces>
rewriteDraft export contract (src/ai/rewriter.js):
```js
// Wave 1 stub: async, returns null always (no network calls)
// Wave 2: calls google/gemini-2.5-flash via OpenRouter, returns {lyrics} or null
export async function rewriteDraft(lyrics, critique) → {lyrics: string} | null
```

runPipeline export contract (src/ai/pipeline.js):
```js
// Wave 1 stub: calls generateLyrics, returns {lyrics, tags, title} (no gates, no rewriter)
// Wave 3: full G→C→R orchestrator with gates, timeouts, sycophancy guard
export async function runPipeline({occasion, genre, mood, voice, wishes}) → {lyrics, tags, title}
```

Config extension (src/config.js, current ai block lines 27-32):
```js
// CURRENT (Phase 2 left it here):
ai: {
  apiKey: process.env.OPENROUTER_API_KEY || '',
  model: process.env.AI_MODEL || 'anthropic/claude-sonnet-4-5',
  baseUrl: process.env.AI_BASE_URL || 'https://openrouter.ai/api/v1',
  criticModel: process.env.AI_CRITIC_MODEL || 'anthropic/claude-sonnet-4.6',
},
// ADD rewriterModel after criticModel:
  rewriterModel: process.env.AI_REWRITER_MODEL || 'google/gemini-2.5-flash',
```

package.json current scripts (lines 7-15):
```json
"check": "...&& node --check src/ai/critic.js",
"test": "node --test src/ai/metrics.test.js src/ai/critic.test.js",
"test:metrics": "node --test src/ai/metrics.test.js",
"test:critic": "node --test src/ai/critic.test.js"
```
Extend to:
```json
"check": "...&& node --check src/ai/critic.js && node --check src/ai/rewriter.js && node --check src/ai/pipeline.js",
"test": "node --test src/ai/metrics.test.js src/ai/critic.test.js src/ai/pipeline.test.js",
"test:metrics": "node --test src/ai/metrics.test.js",
"test:critic": "node --test src/ai/critic.test.js",
"test:pipeline": "node --test src/ai/pipeline.test.js"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create rewriter.js stub + pipeline.js stub + extend config.js + extend package.json</name>
  <files>src/ai/rewriter.js, src/ai/pipeline.js, src/config.js, package.json</files>
  <read_first>
    - src/ai/critic.js (lines 1-20 — import pattern + model constant pattern to replicate)
    - src/config.js (lines 27-32 — current ai block: add rewriterModel after criticModel on line 31)
    - package.json (lines 7-15 — current scripts block to extend)
  </read_first>
  <action>
Create FOUR file changes in this single task. They are inseparable: rewriter.js imports config.ai.rewriterModel, pipeline.js imports rewriter.js, package.json check must include both new files.

**File 1: CREATE `src/ai/rewriter.js`** with this EXACT content:

```js
// Rewrites a song draft given a structured critique from critic.js.
// Uses google/gemini-2.5-flash with thinking mode ON (reasoning.max_tokens = 8000).
// Returns {lyrics} on success, null on failure or exhausted retries.
// Zero new dependencies.
// Wave 1 skeleton — stub returns null so tests run and fail (RED state).
// Wave 2 replaces stub body with real OpenRouter call + REWRITER_SYSTEM_PROMPT.

import { config } from '../config.js';

const REWRITER_MODEL = config.ai.rewriterModel || 'google/gemini-2.5-flash';

/**
 * Rewrites lyrics based on a critique from critiqueDraft().
 * @param {string} lyrics - original song draft
 * @param {object} critique - output of critiqueDraft() from src/ai/critic.js
 * @returns {Promise<{lyrics: string} | null>}
 */
export async function rewriteDraft(lyrics, critique) {
  // Wave 1 stub — returns null. Wave 2 implements the full 2-attempt OpenRouter call.
  void lyrics;
  void critique;
  void REWRITER_MODEL;
  return null;
}
```

**File 2: CREATE `src/ai/pipeline.js`** with this EXACT content:

```js
// Full G→C→R pipeline orchestrator.
// Wraps generateLyrics() → critiqueDraft() → rewriteDraft() with gate logic and timeouts.
// Returns {lyrics, tags, title} in all code paths (same contract as generateLyrics).
// Zero new dependencies.
// Wave 1 skeleton — stub calls generateLyrics and returns {lyrics, tags, title} directly.
// Wave 3 replaces stub with full orchestrator (gates, timeouts, sycophancy guard).

import { generateLyrics } from './client.js';
import { critiqueDraft } from './critic.js';
import { rewriteDraft } from './rewriter.js';

/**
 * Full Generate → Critique → Rewrite pipeline.
 * @param {{occasion: string, genre: string, mood: string, voice: string, wishes: string}} input
 * @returns {Promise<{lyrics: string, tags: string, title: string}>}
 */
export async function runPipeline({ occasion, genre, mood, voice, wishes }) {
  // Wave 1 stub — calls generateLyrics and returns {lyrics, tags, title} directly.
  // Wave 3 replaces with full G→C→R orchestration with gates and timeouts.
  void critiqueDraft;
  void rewriteDraft;
  const draft = await generateLyrics({ occasion, genre, mood, voice, wishes });
  return { lyrics: draft.lyrics, tags: draft.tags, title: draft.title };
}
```

**File 3: MODIFY `src/config.js`** — add ONE line after line 31 (criticModel). Current block:
```js
  ai: {
    apiKey: process.env.OPENROUTER_API_KEY || '',
    model: process.env.AI_MODEL || 'anthropic/claude-sonnet-4-5',
    baseUrl: process.env.AI_BASE_URL || 'https://openrouter.ai/api/v1',
    criticModel: process.env.AI_CRITIC_MODEL || 'anthropic/claude-sonnet-4.6',
  },
```
Replace with:
```js
  ai: {
    apiKey: process.env.OPENROUTER_API_KEY || '',
    model: process.env.AI_MODEL || 'anthropic/claude-sonnet-4-5',
    baseUrl: process.env.AI_BASE_URL || 'https://openrouter.ai/api/v1',
    criticModel: process.env.AI_CRITIC_MODEL || 'anthropic/claude-sonnet-4.6',
    rewriterModel: process.env.AI_REWRITER_MODEL || 'google/gemini-2.5-flash',
  },
```
DO NOT change any other field.

**File 4: MODIFY `package.json`** — replace the entire `scripts` block with:
```json
  "scripts": {
    "start": "node src/index.js",
    "start:tg": "node src/index.js --only=telegram",
    "start:vk": "node src/index.js --only=vk",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/store.js && node --check src/suno/client.js && node --check src/flow/generate.js && node --check src/bots/telegram.js && node --check src/bots/vk.js && node --check src/ai/client.js && node --check src/ai/metrics.js && node --check src/ai/critic.js && node --check src/ai/rewriter.js && node --check src/ai/pipeline.js",
    "test": "node --test src/ai/metrics.test.js src/ai/critic.test.js src/ai/pipeline.test.js",
    "test:metrics": "node --test src/ai/metrics.test.js",
    "test:critic": "node --test src/ai/critic.test.js",
    "test:pipeline": "node --test src/ai/pipeline.test.js"
  },
```
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/rewriter.js` exists and contains `export async function rewriteDraft`
    - `src/ai/pipeline.js` exists and contains `export async function runPipeline`
    - `grep "rewriterModel" src/config.js` matches `rewriterModel: process.env.AI_REWRITER_MODEL || 'google/gemini-2.5-flash'`
    - `grep "test:pipeline" package.json` matches the new script
    - `grep "rewriter.js" package.json` — check script includes src/ai/rewriter.js
    - `npm run check` exits 0 (all files parse cleanly)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pipeline.test.js with 6 RED test cases</name>
  <files>src/ai/pipeline.test.js</files>
  <read_first>
    - src/ai/critic.test.js (lines 1-113 — API key guard, fixtures GENERIC_DRAFT/SPECIFIC_DRAFT/CLEAN_DRAFT, node:test structure to replicate exactly)
    - src/ai/pipeline.js (just created stub — test imports runPipeline from here)
    - src/ai/rewriter.js (just created stub — rewriteDraft returns null, so SC3/SC4 tests fail RED)
  </read_first>
  <action>
CREATE `src/ai/pipeline.test.js` with this EXACT content (6 integration tests, all RED against Wave 1 stubs):

```js
// Интеграционные тесты для src/ai/pipeline.js. Запуск: node --test src/ai/pipeline.test.js
// Покрытие: PIPELINE-01 (full pipeline SC1-SC5), PIPELINE-02 (fast path SC2),
// PIPELINE-04 (KEEP sections SC4), MODELS-01 (rewriter model identity).
// Требует OPENROUTER_API_KEY в .env — реальные API вызовы.

if (!process.env.OPENROUTER_API_KEY) {
  console.log('[pipeline.test] OPENROUTER_API_KEY not set — skipping');
  process.exit(0);
}

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { runPipeline } from './pipeline.js';
import { scoreDraft } from './metrics.js';

// Fixtures — same as critic.test.js (self-contained; no coupling to critic.test.js exports)

const GENERIC_DRAFT = [
  '[Куплет 1]',
  'Он встаёт по утрам и идёт на работу,',
  'Она ждёт его дома с горячим чаем.',
  'Каждый день одно и то же — вот такая суббота,',
  'Так они живут, никуда не уезжая.',
  '[Припев]',
  'Ты мой герой, ты мой идеал,',
  'Ты моя звезда, ты мой свет.',
  'Без тебя бы я не встал,',
  'Дороже тебя в мире нет.',
  '[Куплет 2]',
  'Он сильный и добрый, всегда поддержит,',
  'Она красивая, умная, нежная.',
  'Вместе они пройдут любую нежность,',
  'Их любовь — бесконечная, безбрежная.',
  '[Бридж]',
  'Годы идут, но чувства крепнут,',
  'Счастье живёт в их маленьком доме.',
  'Они никогда не уснут в разлуке,',
  'Их сердца бьются в одном ритме.',
  '[Финал]',
  'Вот такая история любви,',
  'Простая, но честная — живи.',
].join('\n');

const CLEAN_DRAFT = [
  '[Куплет 1]',
  'Шесть утра, а Рома уже шнурует кросс,',
  'Район спит, фонарь горит — и холодно до слёз.',
  'Турник скрипнул, двадцать раз — и выдох в тишину,',
  'Пока соседи спят, считает Рома луну.',
  '[Припев]',
  'С днём рождения, Рома — в путь,',
  'Город спит, а ты — не свернуть.',
  'Впереди всех, и не догнать,',
  'Дорога знает — Рому ей встречать.',
  '[Куплет 2]',
  'В выходной — ружьё, рассвет, болото, грязь,',
  'Три часа в засаде, а потом домой летя.',
  'Мать кричит про сапоги в прихожей нашей,',
  'Рома улыбнётся тихо — мам, ну что ты, Боже...',
  '[Бридж]',
  'Девчонки пишут, а Рома весь в подходе,',
  'Телефон молчит, пока штангу не уронит.',
  'Прочитает позже, усмехнётся — ну ок,',
  'И снова педали — ветер бьёт в висок.',
  '[Финал]',
  'Шесть утра, фонарь, турник — скрип знакомый,',
  'Ещё один рассвет встречает Рома.',
].join('\n');

// Helper: compute ratio of words in rewritten that don't appear in original word set
function tokenizeForDiff(text) {
  return text
    .toLowerCase()
    .replace(/\[[^\]]*\]/g, '')
    .split(/[^а-яёa-z0-9]+/i)
    .filter(w => w.length >= 2);
}

function computeNewTokenRatio(originalLyrics, rewrittenLyrics) {
  const originalWords = new Set(tokenizeForDiff(originalLyrics));
  const rewrittenTokens = tokenizeForDiff(rewrittenLyrics);
  if (rewrittenTokens.length === 0) return 0;
  const newCount = rewrittenTokens.filter(w => !originalWords.has(w)).length;
  return newCount / rewrittenTokens.length;
}

// Helper: extract section text by header name
function extractSection(lyrics, sectionName) {
  const escapedName = sectionName.replace(/[[\]]/g, '\\$&');
  const regex = new RegExp(`${escapedName}\\s*\\n([\\s\\S]*?)(?=\\[|$)`, 'i');
  const match = lyrics.match(regex);
  return match ? match[1].trim().replace(/\s+/g, ' ') : null;
}

// SC1: runPipeline returns valid {lyrics, tags, title} shape for all 3 draft types
describe('runPipeline — output contract (PIPELINE-01 SC1)', () => {
  it('returns {lyrics, tags, title} with non-empty strings for GENERIC_DRAFT', async () => {
    const result = await runPipeline({
      occasion: 'день рождения',
      genre: 'поп',
      mood: 'радостный',
      voice: 'мужской',
      wishes: GENERIC_DRAFT,
    });
    assert.ok(result && typeof result === 'object', 'result must be an object');
    assert.ok(typeof result.lyrics === 'string' && result.lyrics.length > 0, 'lyrics must be non-empty string');
    assert.ok(typeof result.tags === 'string' && result.tags.length > 0, 'tags must be non-empty string');
    assert.ok(typeof result.title === 'string' && result.title.length > 0, 'title must be non-empty string');
    assert.strictEqual(result.metrics, undefined, 'metrics field must NOT appear in runPipeline output');
  });
});

// SC2: draft with skip_pipeline=true triggers fast path (returned lyrics === original generation)
describe('runPipeline — fast path (PIPELINE-02 SC2)', () => {
  it('CLEAN_DRAFT has skip_pipeline=true — pipeline returns without rewriting', async () => {
    const precheck = scoreDraft(CLEAN_DRAFT);
    assert.strictEqual(precheck.skip_pipeline, true,
      'CLEAN_DRAFT must have skip_pipeline=true to test fast path');

    const result = await runPipeline({
      occasion: 'день рождения',
      genre: 'поп',
      mood: 'радостный',
      voice: 'мужской',
      wishes: 'Для Ромы, который встаёт в 6 утра и тренируется.',
    });
    assert.ok(result && typeof result.lyrics === 'string', 'result must have lyrics string');
    // Fast path: the returned lyrics must match the generator output (no rewrite attempted)
    // This is verified indirectly — if skip_pipeline=true, critiqueDraft is not called.
    // Wave 3 adds explicit log assertion: '[pipeline] metrics gate: skip_pipeline=true'
  });
});

// SC3: GENERIC_DRAFT goes through full pipeline, rewrite differs >= 20% new tokens
describe('runPipeline — full rewrite path (PIPELINE-01 SC3)', () => {
  it('GENERIC_DRAFT triggers full pipeline and returned lyrics have >= 20% new tokens', async () => {
    // This test WILL FAIL in Wave 1 because rewriteDraft stub returns null,
    // causing pipeline to fall back to original draft (0% new tokens).
    // Wave 3 makes this GREEN by implementing the full pipeline.
    const precheck = scoreDraft(GENERIC_DRAFT);
    assert.strictEqual(precheck.skip_pipeline, false,
      'GENERIC_DRAFT must NOT have skip_pipeline=true');

    const originalLyrics = GENERIC_DRAFT;
    const result = await runPipeline({
      occasion: 'день рождения',
      genre: 'поп',
      mood: 'радостный',
      voice: 'мужской',
      wishes: 'Это просто тест пайплайна.',
    });
    const ratio = computeNewTokenRatio(originalLyrics, result.lyrics);
    assert.ok(ratio >= 0.20,
      `rewritten lyrics must have >= 20% new tokens; got ${(ratio * 100).toFixed(1)}%`);
  });
});

// SC4: KEEP sections from critique appear verbatim in rewritten lyrics
describe('runPipeline — KEEP sections verbatim (PIPELINE-04 SC4)', () => {
  it('sections marked KEEP in critique are reproduced verbatim in returned lyrics', async () => {
    // Import critique separately to get keep_sections list
    const { critiqueDraft } = await import('./critic.js');
    const metrics = scoreDraft(GENERIC_DRAFT);
    const critique = await critiqueDraft(GENERIC_DRAFT, metrics);
    if (!critique || critique.total >= 12) {
      // Skip if critique unavailable or fast path would trigger
      console.log('[SC4] skipping: critique null or total>=12');
      return;
    }

    const result = await runPipeline({
      occasion: 'день рождения',
      genre: 'поп',
      mood: 'радостный',
      voice: 'мужской',
      wishes: 'Это просто тест пайплайна.',
    });

    for (const sectionHeader of critique.keep_sections) {
      const origSection = extractSection(GENERIC_DRAFT, sectionHeader);
      const rewriteSection = extractSection(result.lyrics, sectionHeader);
      if (origSection && rewriteSection) {
        assert.strictEqual(rewriteSection, origSection,
          `KEEP section "${sectionHeader}" must be reproduced verbatim`);
      }
    }
  });
});

// SC5: rewriteDraft returns {lyrics} (non-null) when called with a low-scoring critique
describe('rewriteDraft — direct call (MODELS-01 / PIPELINE-04 SC5)', () => {
  it('rewriteDraft returns {lyrics} string when called with GENERIC_DRAFT and a valid critique', async () => {
    // This test WILL FAIL in Wave 1 because rewriteDraft stub returns null.
    // Wave 2 makes this GREEN by implementing the full rewriter call.
    const { rewriteDraft } = await import('./rewriter.js');
    const { critiqueDraft } = await import('./critic.js');
    const metrics = scoreDraft(GENERIC_DRAFT);
    const critique = await critiqueDraft(GENERIC_DRAFT, metrics);
    if (!critique) {
      console.log('[SC5] skipping: critique null');
      return;
    }

    const result = await rewriteDraft(GENERIC_DRAFT, critique);
    assert.ok(result !== null, 'rewriteDraft must not return null for a valid low-scoring critique');
    assert.ok(typeof result.lyrics === 'string' && result.lyrics.length > 0,
      'rewriteDraft must return {lyrics} with non-empty string');
  });
});

// Unit: tokenizeForDiff and computeNewTokenRatio work correctly for Russian text
describe('computeNewTokenRatio — unit test', () => {
  it('returns correct ratio for Russian texts with known word overlap', () => {
    const original = 'Он встаёт по утрам и идёт на работу каждый день';
    const rewritten = 'Антон бежит сквозь ночь по знакомым переулкам города';
    const ratio = computeNewTokenRatio(original, rewritten);
    // "антон", "бежит", "сквозь", "ночь", "знакомым", "переулкам", "города" are new (7 of ~8 tokens)
    assert.ok(ratio >= 0.80,
      `ratio should be >= 0.80 for mostly-new text; got ${(ratio * 100).toFixed(1)}%`);

    const identical = computeNewTokenRatio(original, original);
    assert.strictEqual(identical, 0, 'identical texts must have 0% new tokens');
  });
});
```
  </action>
  <verify>
    <automated>node --check src/ai/pipeline.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/pipeline.test.js` exists
    - `node --check src/ai/pipeline.test.js` exits 0
    - `grep "OPENROUTER_API_KEY" src/ai/pipeline.test.js` matches the guard at line 7
    - `grep "runPipeline" src/ai/pipeline.test.js` — at least 5 occurrences
    - `grep "GENERIC_DRAFT" src/ai/pipeline.test.js` — fixture present
    - `grep "computeNewTokenRatio" src/ai/pipeline.test.js` — sycophancy helper present
    - `npm run check` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Plan 01 Verification

Run after both tasks complete:

```bash
npm run check
```

Expected: exits 0 — all 5 new/modified files parse cleanly.

```bash
node -e "import('./src/config.js').then(m => console.log('rewriterModel:', m.config.ai.rewriterModel))"
```
Expected: `rewriterModel: google/gemini-2.5-flash`

```bash
node -e "import('./src/ai/rewriter.js').then(async m => { const r = await m.rewriteDraft('x', {}); console.log('rewriteDraft stub:', r); })"
```
Expected: `rewriteDraft stub: null`

```bash
node -e "import('./src/ai/pipeline.js').then(m => console.log('exports:', Object.keys(m)))"
```
Expected: `exports: [ 'runPipeline' ]`

RED state confirmation (requires OPENROUTER_API_KEY):
```bash
node --test src/ai/pipeline.test.js 2>&1 | grep -E "FAIL|pass|fail"
```
Expected: tests for SC3 and SC5 FAIL (rewriteDraft returns null in Wave 1 stub).
Tests for SC1, SC2, SC6 should PASS (runPipeline stub works, computeNewTokenRatio works).
</verification>
