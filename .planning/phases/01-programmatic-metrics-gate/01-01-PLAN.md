---
phase: 01-programmatic-metrics-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ai/metrics.js
  - src/ai/metrics.test.js
autonomous: true
requirements:
  - METRICS-01
  - METRICS-02
  - METRICS-04
must_haves:
  truths:
    - "An empty `src/ai/metrics.js` skeleton exports a `scoreDraft` function (callable, returns object)"
    - "A `src/ai/metrics.test.js` file exists with at least 7 failing test cases covering banale, syllables, MATTR, and gate"
    - "Running `node --test src/ai/metrics.test.js` exits non-zero (RED state — implementation pending)"
    - "`node --check src/ai/metrics.js` and `node --check src/ai/metrics.test.js` both pass (valid ESM syntax)"
  artifacts:
    - path: "src/ai/metrics.js"
      provides: "scoreDraft() skeleton export — returns placeholder shape"
      min_lines: 15
      contains: "export function scoreDraft"
    - path: "src/ai/metrics.test.js"
      provides: "Failing test stubs for METRICS-01, METRICS-02, METRICS-04, gate"
      min_lines: 60
      contains: "import { scoreDraft }"
  key_links:
    - from: "src/ai/metrics.test.js"
      to: "src/ai/metrics.js"
      via: "ESM import"
      pattern: "from '\\./metrics\\.js'"
---

<objective>
Create the Wave 0 test scaffold and module skeleton for Phase 1 (Programmatic Metrics Gate).
This plan establishes the RED state of TDD: the test file exists with concrete assertions
covering METRICS-01 (banale rhymes), METRICS-02 (chorus syllables), METRICS-04 (MATTR
diversity), and the `skip_pipeline` gate, while `src/ai/metrics.js` is only a callable
skeleton. Plan 02 turns the tests GREEN by implementing the algorithms.

Purpose: VALIDATION.md (Wave 0 Requirements) mandates that `src/ai/metrics.test.js`
exists before any implementation runs, so every implementation task in Plan 02 has
an automated `<verify>` command that already exists.
Output: Two new files — a non-implemented module and a comprehensive failing test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-programmatic-metrics-gate/01-RESEARCH.md
@.planning/phases/01-programmatic-metrics-gate/01-PATTERNS.md
@.planning/phases/01-programmatic-metrics-gate/01-VALIDATION.md
@src/access-codes.js
@src/ai/client.js
@package.json

<interfaces>
<!-- Contract that Plan 02 will implement against. All test assertions are written to this contract. -->

The single export of src/ai/metrics.js (defined here as a stub, implemented in Plan 02):

```js
/**
 * @param {string} lyrics - full lyrics text with [Куплет 1], [Припев], etc. section tags
 * @returns {{
 *   banale_pairs: string[][],          // each entry is [word1, word2] from same banale cluster
 *   syllable_violations: Array<{ line: string, count: number, max: number }>,
 *   lexical_diversity: number,         // 0.000–1.000, rounded to 3 decimals
 *   skip_pipeline: boolean             // true when banale_pairs=[] AND violations=[] AND diversity>=0.60
 * }}
 */
export function scoreDraft(lyrics) { ... }
```

Closest project analog (read first): `src/access-codes.js` — module-level constant data
structure (CODES object → wordToCluster Map), named exports only, JSDoc on the export,
zero imports, log tag prefix `[module]` only when caller logs (this module stays silent).
</interfaces>

<test_runtime>
- Framework: `node:test` (Node 22 built-in, no devDependency required).
- Imports in test file: `import { describe, it } from 'node:test'; import assert from 'node:assert/strict'; import { scoreDraft } from './metrics.js';`
- Run: `node --test src/ai/metrics.test.js` (exit 0 = pass, non-zero = fail).
- File location: co-located with source per VALIDATION.md (NOT `tests/` per RESEARCH.md — VALIDATION.md is authoritative for test file paths).
</test_runtime>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/ai/metrics.js skeleton with scoreDraft stub</name>
  <files>src/ai/metrics.js</files>
  <read_first>
    - src/access-codes.js (canonical analog — copy its file-header style, JSDoc style, named-export style)
    - .planning/phases/01-programmatic-metrics-gate/01-PATTERNS.md (Section "src/ai/metrics.js" subsections)
    - .planning/phases/01-programmatic-metrics-gate/01-RESEARCH.md (Pattern 5: Gate Function — return shape)
  </read_first>
  <behavior>
    - Importing `scoreDraft` from `./metrics.js` succeeds without runtime error.
    - Calling `scoreDraft('any text')` returns a plain object whose own keys are exactly: `banale_pairs`, `syllable_violations`, `lexical_diversity`, `skip_pipeline`.
    - All four returned values exist as the right primitive shape: arrays for `banale_pairs` and `syllable_violations`, number for `lexical_diversity`, boolean for `skip_pipeline`.
    - Returned values are placeholder defaults (empty arrays, 0, false) — implementation is intentionally absent and will be filled in Plan 02.
    - `node --check src/ai/metrics.js` exits 0 (valid syntax).
  </behavior>
  <action>
Create the file `src/ai/metrics.js` with this exact structure (no other content):

```js
// Синхронный gate-модуль для оценки качества черновика текста песни.
// Три метрики: банальные рифмы (lookup по кластерам), слоговые нарушения (regex по гласным),
// лексическое разнообразие (MATTR-approx, скользящее окно 50 токенов).
// Нет зависимостей, нет I/O, нет API-вызовов. Чистая функция.

// === SKELETON ONLY ===
// Реализация метрик переносится в Plan 02. Этот файл существует, чтобы
// тесты в src/ai/metrics.test.js могли импортировать scoreDraft и запускаться (RED state TDD).

/**
 * Оценивает черновик текста песни по трём метрикам качества.
 * Синхронная функция, без I/O, без зависимостей.
 *
 * @param {string} lyrics - полный текст с секциями [Куплет 1], [Припев], [Куплет 2], [Бридж], [Финал]
 * @returns {{ banale_pairs: string[][], syllable_violations: Array<{line: string, count: number, max: number}>, lexical_diversity: number, skip_pipeline: boolean }}
 */
export function scoreDraft(lyrics) {
  // PLACEHOLDER — implementation lands in Plan 02 (METRICS-01, METRICS-02, METRICS-04 + gate).
  // Returning a shape-correct stub keeps imports working while tests stay RED.
  return {
    banale_pairs: [],
    syllable_violations: [],
    lexical_diversity: 0,
    skip_pipeline: false,
  };
}
```

Conventions enforced (per .planning/phases/01-programmatic-metrics-gate/01-PATTERNS.md and CONVENTIONS.md):
- 2-space indentation
- Single quotes
- Semicolons at statement end
- ESM (project package.json has `"type": "module"`)
- Named export only — no `export default`
- File-level Russian comment block describing module purpose
- JSDoc on the exported function
- Zero imports (pure JS, Node built-ins not needed for skeleton)
  </action>
  <verify>
    <automated>node --check "src/ai/metrics.js" &amp;&amp; node -e "import('./src/ai/metrics.js').then(m =&gt; { const r = m.scoreDraft('test'); const k = Object.keys(r).sort().join(','); if (k !== 'banale_pairs,lexical_diversity,skip_pipeline,syllable_violations') { console.error('Bad shape:', k); process.exit(1); } if (!Array.isArray(r.banale_pairs) || !Array.isArray(r.syllable_violations) || typeof r.lexical_diversity !== 'number' || typeof r.skip_pipeline !== 'boolean') { console.error('Bad types'); process.exit(1); } console.log('OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ai/metrics.js` exists.
    - `node --check src/ai/metrics.js` exits 0.
    - `grep -c "export function scoreDraft" src/ai/metrics.js` returns at least 1.
    - `grep -c "export default" src/ai/metrics.js` returns 0 (no default exports).
    - Calling `scoreDraft('x')` returns object with exactly these own keys (sorted): `banale_pairs,lexical_diversity,skip_pipeline,syllable_violations`.
    - `typeof returned.lexical_diversity === 'number'` and `typeof returned.skip_pipeline === 'boolean'`.
    - File contains a Russian file-header comment block (3+ lines starting with `//`).
    - File contains a JSDoc block (`/** ... */`) immediately above the `export function scoreDraft` line.
  </acceptance_criteria>
  <done>
    Skeleton module file exists, exports `scoreDraft` with the contract from `<interfaces>`, returns a shape-correct stub, and passes Node syntax check.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/ai/metrics.test.js with failing test suite</name>
  <files>src/ai/metrics.test.js</files>
  <read_first>
    - src/ai/metrics.js (the skeleton from Task 1 — confirm scoreDraft import path)
    - .planning/phases/01-programmatic-metrics-gate/01-RESEARCH.md (section "Test Structure (node:test)" — sample test layout)
    - .planning/phases/01-programmatic-metrics-gate/01-VALIDATION.md (Per-Task Verification Map — tests must cover METRICS-01, 02, 04, gate)
  </read_first>
  <behavior>
    - File imports `{ describe, it }` from `node:test`, `assert` from `node:assert/strict`, and `{ scoreDraft }` from `./metrics.js`.
    - Suite contains EXACTLY four describe blocks: `'banale detection (METRICS-01)'`, `'syllable violations (METRICS-02)'`, `'lexical diversity / MATTR (METRICS-04)'`, `'skip_pipeline gate'`.
    - Total `it()` cases: at least 7 (≥1 in banale, ≥1 in syllable, ≥2 in MATTR for low/high, ≥2 in gate for skip-true/skip-false, plus ≥1 round-trip shape test).
    - Running `node --test src/ai/metrics.test.js` exits non-zero (RED state — at least one assertion fails because implementation is a stub).
    - `node --check src/ai/metrics.test.js` exits 0 (valid syntax).
    - File does NOT use any test runner other than `node:test` (no jest, no vitest, no mocha).
  </behavior>
  <action>
Create the file `src/ai/metrics.test.js` with this exact content. The tests are written
to the contract published in `<interfaces>` above and stay valid through Plan 02's
implementation. They are deliberately failing now because `scoreDraft` is a stub.

```js
// Юнит-тесты для src/ai/metrics.js. Запуск: node --test src/ai/metrics.test.js
// Покрытие: METRICS-01 (банальные рифмы), METRICS-02 (слоги припева),
// METRICS-04 (MATTR-approx), и поведение gate skip_pipeline.

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { scoreDraft } from './metrics.js';

// Чистый черновик: разнообразный, без банальных пар, припев в пределах 12 слогов.
// Используется в gate-тесте skip_pipeline=true.
const CLEAN_DRAFT = [
  '[Куплет 1]',
  'Шесть утра, а Рома уже шнурует кросс,',
  'Район спит, фонарь горит — и холодно до слёз.',
  'Турник скрипнул, двадцать раз — и выдох в тишину,',
  'Пока соседи спят, считает Рома луну.',
  '[Припев]',
  'С днём рождения, Рома — в путь,',
  'Город спит, а ты — не свернуть.',
  'Впереди всех, и не догнать,',
  'Дорога знает — Рому ей встречать.',
  '[Куплет 2]',
  'В выходной — ружьё, рассвет, болото, грязь,',
  'Три часа в засаде, а потом домой летя.',
  'Мать кричит про сапоги в прихожей нашей,',
  'Рома улыбнётся тихо — мам, ну что ты, Боже...',
  '[Бридж]',
  'Девчонки пишут, а Рома весь в подходе,',
  'Телефон молчит, пока штангу не уронит.',
  'Прочитает позже, усмехнётся — ну ок,',
  'И снова педали — ветер бьёт в висок.',
  '[Финал]',
  'Шесть утра, фонарь, турник — скрип знакомый,',
  'Ещё один рассвет встречает Рома.',
].join('\n');

describe('banale detection (METRICS-01)', () => {
  it('detects любовь/кровь pair from canonical cluster', () => {
    const lyrics = [
      '[Куплет 1]',
      'Я пишу тебе про любовь',
      'А в висках стучит кровь',
    ].join('\n');
    const result = scoreDraft(lyrics);
    assert.ok(Array.isArray(result.banale_pairs), 'banale_pairs must be array');
    assert.ok(result.banale_pairs.length >= 1, 'expected at least one banale pair');
    const flat = result.banale_pairs.flat();
    assert.ok(flat.includes('любовь'), 'expected любовь in flat pairs');
    assert.ok(flat.includes('кровь'), 'expected кровь in flat pairs');
  });

  it('detects розы/слёзы from morose cluster', () => {
    const lyrics = [
      '[Куплет 1]',
      'Дарю тебе розы',
      'А в глазах стоят слёзы',
    ].join('\n');
    const result = scoreDraft(lyrics);
    const flat = result.banale_pairs.flat();
    assert.ok(flat.includes('розы') && flat.includes('слёзы'),
      'expected розы/слёзы pair detected');
  });
});

describe('syllable violations (METRICS-02)', () => {
  it('flags chorus line over 12 syllables', () => {
    // 'радиоактивный город заснял один фотограф у моря' = 18 vowels (syllables)
    const lyrics = [
      '[Куплет 1]',
      'Короткая строка',
      '[Припев]',
      'радиоактивный город заснял один фотограф у моря',
    ].join('\n');
    const result = scoreDraft(lyrics);
    assert.ok(Array.isArray(result.syllable_violations));
    assert.ok(result.syllable_violations.length >= 1, 'expected violation');
    const v = result.syllable_violations[0];
    assert.ok(v.count > 12, `expected count > 12 got ${v.count}`);
    assert.ok(typeof v.line === 'string' && v.line.length > 0);
  });

  it('does not flag chorus line at exactly 12 syllables', () => {
    // 'мама папа я сестра брат друг любовь' = 11 vowels — under limit
    const lyrics = [
      '[Куплет 1]',
      'Тут ничего нет',
      '[Припев]',
      'мама папа я сестра брат друг любовь',
    ].join('\n');
    const result = scoreDraft(lyrics);
    assert.strictEqual(result.syllable_violations.length, 0,
      'expected no violations for short chorus line');
  });
});

describe('lexical diversity / MATTR (METRICS-04)', () => {
  it('repetitive text scores below 0.60', () => {
    const repeated = Array(60).fill('слово другое').join(' ');
    const lyrics = `[Куплет 1]\n${repeated}`;
    const result = scoreDraft(lyrics);
    assert.ok(typeof result.lexical_diversity === 'number');
    assert.ok(result.lexical_diversity < 0.60,
      `expected < 0.60, got ${result.lexical_diversity}`);
  });

  it('varied text scores at or above 0.60', () => {
    const result = scoreDraft(CLEAN_DRAFT);
    assert.ok(result.lexical_diversity >= 0.60,
      `expected >= 0.60 for varied draft, got ${result.lexical_diversity}`);
  });
});

describe('skip_pipeline gate', () => {
  it('returns shape-correct object on any input (smoke test)', () => {
    const r = scoreDraft('');
    assert.deepStrictEqual(
      Object.keys(r).sort(),
      ['banale_pairs', 'lexical_diversity', 'skip_pipeline', 'syllable_violations'],
      'return shape must match contract'
    );
  });

  it('returns skip_pipeline=true for clean varied draft', () => {
    const result = scoreDraft(CLEAN_DRAFT);
    assert.strictEqual(result.banale_pairs.length, 0,
      `clean draft should have no banale pairs, got ${JSON.stringify(result.banale_pairs)}`);
    assert.strictEqual(result.syllable_violations.length, 0,
      'clean draft should have no syllable violations');
    assert.ok(result.lexical_diversity >= 0.60,
      `clean draft diversity should be >= 0.60, got ${result.lexical_diversity}`);
    assert.strictEqual(result.skip_pipeline, true,
      'all checks passed → skip_pipeline must be true');
  });

  it('returns skip_pipeline=false when banale pair found', () => {
    const lyrics = [
      '[Куплет 1]',
      'Я пишу тебе про любовь',
      'А в висках стучит кровь',
    ].join('\n');
    const result = scoreDraft(lyrics);
    assert.strictEqual(result.skip_pipeline, false,
      'banale pair present → skip_pipeline must be false');
  });
});
```

Conventions:
- 2-space indentation, single quotes, semicolons.
- ESM imports with `.js` extensions.
- Russian comments and string literals; English variable names.
  </action>
  <verify>
    <automated>node --check "src/ai/metrics.test.js" &amp;&amp; (node --test "src/ai/metrics.test.js"; if [ $? -eq 0 ]; then echo "FAIL: tests should be RED but exited 0"; exit 1; else echo "OK: tests RED as expected"; exit 0; fi)</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ai/metrics.test.js` exists.
    - `node --check src/ai/metrics.test.js` exits 0.
    - `grep -c "from 'node:test'" src/ai/metrics.test.js` returns at least 1.
    - `grep -c "from 'node:assert/strict'" src/ai/metrics.test.js` returns at least 1.
    - `grep -c "from './metrics.js'" src/ai/metrics.test.js` returns at least 1.
    - `grep -c "describe(" src/ai/metrics.test.js` returns at least 4 (one per dimension).
    - `grep -c "it(" src/ai/metrics.test.js` returns at least 7.
    - `grep -c "METRICS-01" src/ai/metrics.test.js` returns at least 1.
    - `grep -c "METRICS-02" src/ai/metrics.test.js` returns at least 1.
    - `grep -c "METRICS-04" src/ai/metrics.test.js` returns at least 1.
    - Running `node --test src/ai/metrics.test.js` exits non-zero (RED state — proves tests actually assert against unimplemented logic, not vacuously pass).
    - File contains no `import` from `jest`, `vitest`, `mocha`, or `chai`.
  </acceptance_criteria>
  <done>
    Test file with seven+ failing assertions across four describe blocks committed; running `node --test` exits non-zero, confirming Wave 0 RED state for Plan 02 to flip GREEN.
  </done>
</task>

</tasks>

<verification>
Combined Wave 0 health check:
- Both files exist: `src/ai/metrics.js`, `src/ai/metrics.test.js`.
- Both pass `node --check` (valid ESM syntax).
- Test runner detects the suite: `node --test src/ai/metrics.test.js` produces TAP output with at least 7 test cases.
- Suite is RED (exit non-zero) — confirms tests assert against real behavior.
- `scoreDraft('any')` returns the contract shape (arrays, number, boolean).
</verification>

<success_criteria>
1. `node --check src/ai/metrics.js` → exit 0
2. `node --check src/ai/metrics.test.js` → exit 0
3. `node --test src/ai/metrics.test.js` → exit non-zero with TAP output naming the four describe blocks
4. `grep -c "describe(" src/ai/metrics.test.js` >= 4
5. `grep -c "it(" src/ai/metrics.test.js` >= 7
6. `grep -c "export function scoreDraft" src/ai/metrics.js` == 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-programmatic-metrics-gate/01-01-SUMMARY.md`
documenting: files created, exact test count, exact RED-state proof (TAP output excerpt
showing failing assertions), and confirmation that Plan 02 can begin implementation.
</output>
