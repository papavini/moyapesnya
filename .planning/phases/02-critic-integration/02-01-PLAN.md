---
phase: 02-critic-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/critic.js
  - src/ai/critic.test.js
  - src/config.js
  - package.json
autonomous: true
requirements:
  - PIPELINE-03
  - MODELS-02
  - METRICS-03
must_haves:
  truths:
    - "src/ai/critic.js exists and exports judgeSpecificity and critiqueDraft as async functions (callable stubs are acceptable in this wave)"
    - "src/ai/critic.test.js exists with all 6 RED test cases for PIPELINE-03 / METRICS-03 / MODELS-02"
    - "config.ai.criticModel is defined and defaults to 'anthropic/claude-sonnet-4.6' (dot notation)"
    - "package.json test script invokes both metrics.test.js and critic.test.js"
    - "npm run check passes (critic.js parses cleanly)"
    - "Wave 1 tests fail (RED state) when run against the stubs — confirming the test contract is real"
  artifacts:
    - path: "src/ai/critic.js"
      provides: "module skeleton with two exported async stubs and shared module constants"
      exports: ["judgeSpecificity", "critiqueDraft"]
      contains: "anthropic/claude-sonnet-4.6"
    - path: "src/ai/critic.test.js"
      provides: "6 integration test cases (RED initially) covering specificity judge + 5-dimension critique"
      contains: "GENERIC_DRAFT"
    - path: "src/config.js"
      provides: "config.ai.criticModel field"
      contains: "criticModel"
    - path: "package.json"
      provides: "test scripts wired to critic.test.js"
      contains: "test:critic"
  key_links:
    - from: "src/ai/critic.test.js"
      to: "src/ai/critic.js"
      via: "import { judgeSpecificity, critiqueDraft } from './critic.js'"
      pattern: "from '\\./critic\\.js'"
    - from: "src/ai/critic.test.js"
      to: "src/ai/metrics.js"
      via: "import { scoreDraft } from './metrics.js' for the Phase 1 gate fixture"
      pattern: "from '\\./metrics\\.js'"
    - from: "src/ai/critic.js"
      to: "src/config.js"
      via: "import { config } from '../config.js'"
      pattern: "from '\\.\\./config\\.js'"
---

<objective>
Create the Phase 2 critic module skeleton, the test contract, the config field, and the npm
script wiring. After this plan completes, the test suite must run and FAIL (RED state) because
the stubs return shape-incompatible defaults — Wave 2 then turns the tests GREEN by replacing
the stubs with real implementations.

Purpose: Lock the contract before writing the OpenRouter calls. The tests define the exact
shape of `judgeSpecificity` and `critiqueDraft`, the model identity (`anthropic/claude-sonnet-4.6`),
and the config wiring. Wave 2 implements against this contract — no shape ambiguity.

Output: Four file changes — one new module, one new test file, one config edit, one
package.json edit. No external API calls in this wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-critic-integration/02-RESEARCH.md
@.planning/phases/02-critic-integration/02-PATTERNS.md
@.planning/phases/02-critic-integration/02-VALIDATION.md
@src/ai/client.js
@src/ai/metrics.js
@src/ai/metrics.test.js
@src/config.js
@package.json
</context>

<interfaces>
<!-- Contracts the executor MUST honor. Extracted verbatim from RESEARCH.md and PATTERNS.md. -->

Critic module exports (src/ai/critic.js):
```js
// Async — wraps OpenRouter call to anthropic/claude-sonnet-4.6 (specificity micro-call)
// Wave 1 stub: return a fixed object { has_proper_nouns: false, has_time_expressions: false }
//             so the test files load and tests RUN (and FAIL on the assertions).
export async function judgeSpecificity(lyrics: string): Promise<{
  has_proper_nouns: boolean,
  has_time_expressions: boolean,
}>;

// Async — wraps the 5-dimension critique call.
// Wave 1 stub: return null so tests RUN (the parseCritique-shape assertions FAIL).
export async function critiqueDraft(lyrics: string, metrics: object): Promise<null | {
  story_specificity: { score: number, rewrite_instructions: string },
  chorus_identity:   { score: number, rewrite_instructions: string },
  rhyme_quality:     { score: number, rewrite_instructions: string },
  singability:       { score: number, rewrite_instructions: string },
  emotional_honesty: { score: number, rewrite_instructions: string },
  total: number,
  keep_sections: string[],
}>;
```

Existing scoreDraft contract from src/ai/metrics.js (input to critiqueDraft via the test):
```js
// Returns synchronously — no I/O
export function scoreDraft(lyrics: string): {
  banale_pairs: string[][],                                            // e.g. [['любовь','кровь']]
  syllable_violations: Array<{line: string, count: number, max: 12}>,
  lexical_diversity: number,                                           // 0..1
  skip_pipeline: boolean,
};
```

Config block to extend (src/config.js, current shape):
```js
ai: {
  apiKey: process.env.OPENROUTER_API_KEY || '',
  model: process.env.AI_MODEL || 'anthropic/claude-sonnet-4-5',
  baseUrl: process.env.AI_BASE_URL || 'https://openrouter.ai/api/v1',
  // ADD next line after baseUrl:
  criticModel: process.env.AI_CRITIC_MODEL || 'anthropic/claude-sonnet-4.6',
},
```

Existing package.json scripts shape (verbatim from current file lines 7-14):
```json
"scripts": {
  "start": "node src/index.js",
  "start:tg": "node src/index.js --only=telegram",
  "start:vk": "node src/index.js --only=vk",
  "check": "node --check src/index.js && node --check src/config.js && node --check src/store.js && node --check src/suno/client.js && node --check src/flow/generate.js && node --check src/bots/telegram.js && node --check src/bots/vk.js && node --check src/ai/client.js && node --check src/ai/metrics.js",
  "test": "node --test src/ai/metrics.test.js",
  "test:metrics": "node --test src/ai/metrics.test.js"
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/ai/critic.js skeleton + extend src/config.js + extend package.json scripts</name>
  <files>src/ai/critic.js, src/config.js, package.json</files>
  <read_first>
    - src/ai/client.js (lines 1-3 import pattern; lines 268-323 retry/parse pattern — copy structure later in Wave 2)
    - src/ai/metrics.js (lines 1-4 header comment style; line 254 named export style)
    - src/config.js (lines 13-42 — current `ai` block at lines 27-31)
    - package.json (lines 7-14 — current scripts block)
    - .planning/phases/02-critic-integration/02-PATTERNS.md (sections "src/ai/critic.js", "src/config.js", "package.json")
  </read_first>
  <behavior>
    - critic.js loads cleanly under `node --check src/ai/critic.js`
    - Importing the module yields two async function exports: `judgeSpecificity` and `critiqueDraft`
    - Calling the stubs does NOT make any network call (they return fixed defaults synchronously after a `Promise.resolve` wrap)
    - `config.ai.criticModel` equals `'anthropic/claude-sonnet-4.6'` when `AI_CRITIC_MODEL` env is unset
    - `config.ai.criticModel` honours an `AI_CRITIC_MODEL` env override
    - `npm test` invokes BOTH `src/ai/metrics.test.js` AND `src/ai/critic.test.js`
    - `npm run test:critic` invokes ONLY `src/ai/critic.test.js`
    - `npm run check` includes `src/ai/critic.js` and passes
  </behavior>
  <action>
Create THREE file changes in this single task. They are inseparable: critic.js stub references `config.ai.criticModel`, package.json `check` script must include critic.js, and `test` script must invoke critic.test.js — splitting them produces a non-runnable intermediate state.

**File 1: CREATE `src/ai/critic.js`** with this EXACT content (Wave 1 skeleton):

```js
// Two-call LLM critic: specificity judge (METRICS-03) + 5-dimension critique (PIPELINE-03).
// Uses anthropic/claude-sonnet-4.6 via OpenRouter. No new dependencies.
// Wave 1 skeleton — stubs return fixed defaults so tests run and fail (RED state).
// Wave 2 replaces stub bodies with real OpenRouter calls.

import { config } from '../config.js';

// [VERIFIED: openrouter.ai/anthropic/claude-sonnet-4.6 — dot notation, released 2026-02-17]
// Note: dot notation is OpenRouter namespace; Anthropic API uses hyphens (claude-sonnet-4-6).
// Hardcoded with optional env override via config.ai.criticModel.
const CRITIC_MODEL = config.ai.criticModel || 'anthropic/claude-sonnet-4.6';

/**
 * METRICS-03 — separate micro-call: does the draft contain proper nouns and time expressions?
 * @param {string} lyrics
 * @returns {Promise<{has_proper_nouns: boolean, has_time_expressions: boolean}>}
 */
export async function judgeSpecificity(lyrics) {
  // Wave 1 stub — returns fixed defaults; Wave 2 replaces with OpenRouter micro-call.
  // Must be async so the export shape matches Wave 2 implementation.
  void lyrics; // mark parameter as intentionally unused in stub
  void CRITIC_MODEL; // referenced so import is not dead code
  return { has_proper_nouns: false, has_time_expressions: false };
}

/**
 * PIPELINE-03 — 5-dimension critique. Caller passes Phase 1 metrics from scoreDraft().
 * Wave 1 stub returns null (caller treats as "skip pipeline"); Wave 2 returns critique JSON.
 * @param {string} lyrics
 * @param {object} metrics - output of scoreDraft() from src/ai/metrics.js
 * @returns {Promise<null | object>}
 */
export async function critiqueDraft(lyrics, metrics) {
  // Wave 1 stub — returns null. Wave 2 implements the full 2-attempt OpenRouter call.
  void lyrics;
  void metrics;
  return null;
}
```

**File 2: MODIFY `src/config.js`** — add ONE line to the `ai` block. Current block (lines 27-31):
```js
  ai: {
    apiKey: process.env.OPENROUTER_API_KEY || '',
    model: process.env.AI_MODEL || 'anthropic/claude-sonnet-4-5',
    baseUrl: process.env.AI_BASE_URL || 'https://openrouter.ai/api/v1',
  },
```
Replace with:
```js
  ai: {
    apiKey: process.env.OPENROUTER_API_KEY || '',
    model: process.env.AI_MODEL || 'anthropic/claude-sonnet-4-5',
    baseUrl: process.env.AI_BASE_URL || 'https://openrouter.ai/api/v1',
    criticModel: process.env.AI_CRITIC_MODEL || 'anthropic/claude-sonnet-4.6',
  },
```
DO NOT change the existing `model` default (`anthropic/claude-sonnet-4-5`) — that is the generator default and is owned by Phase 3. ONLY add the `criticModel` line. The dot notation in `'anthropic/claude-sonnet-4.6'` is required (OpenRouter namespace) — RESEARCH Pitfall 1 covers this.

**File 3: MODIFY `package.json`** — replace the entire `scripts` block (current lines 7-14) with:
```json
  "scripts": {
    "start": "node src/index.js",
    "start:tg": "node src/index.js --only=telegram",
    "start:vk": "node src/index.js --only=vk",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/store.js && node --check src/suno/client.js && node --check src/flow/generate.js && node --check src/bots/telegram.js && node --check src/bots/vk.js && node --check src/ai/client.js && node --check src/ai/metrics.js && node --check src/ai/critic.js",
    "test": "node --test src/ai/metrics.test.js src/ai/critic.test.js",
    "test:metrics": "node --test src/ai/metrics.test.js",
    "test:critic": "node --test src/ai/critic.test.js"
  },
```
Three changes inside that block:
  1. `check` ends with `&& node --check src/ai/critic.js`
  2. `test` adds `src/ai/critic.test.js` (space-separated second arg)
  3. New entry `test:critic` runs only the critic test file
Do NOT modify `dependencies`, `engines`, or any field outside `scripts`.
  </action>
  <verify>
    <automated>node --check src/ai/critic.js && node --check src/config.js && node -e "import('./src/config.js').then(m => { if (m.config.ai.criticModel !== 'anthropic/claude-sonnet-4.6') { console.error('FAIL: criticModel default wrong: ' + m.config.ai.criticModel); process.exit(1); } console.log('OK criticModel default'); })" && node -e "import('./src/ai/critic.js').then(async m => { if (typeof m.judgeSpecificity !== 'function') { console.error('FAIL: judgeSpecificity missing'); process.exit(1); } if (typeof m.critiqueDraft !== 'function') { console.error('FAIL: critiqueDraft missing'); process.exit(1); } const s = await m.judgeSpecificity('x'); if (typeof s.has_proper_nouns !== 'boolean' || typeof s.has_time_expressions !== 'boolean') { console.error('FAIL: judgeSpecificity shape wrong'); process.exit(1); } const c = await m.critiqueDraft('x', {}); if (c !== null) { console.error('FAIL: critiqueDraft stub must return null in Wave 1'); process.exit(1); } console.log('OK exports + stub shapes'); })" && node -e "const p = require('./package.json'); if (!p.scripts.test.includes('critic.test.js')) { console.error('FAIL: test script missing critic.test.js'); process.exit(1); } if (!p.scripts['test:critic']) { console.error('FAIL: test:critic script missing'); process.exit(1); } if (!p.scripts.check.includes('src/ai/critic.js')) { console.error('FAIL: check script missing critic.js'); process.exit(1); } console.log('OK package.json scripts'); "</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/ai/critic.js` exits 0
    - `node --check src/config.js` exits 0
    - Importing `./src/config.js` yields `config.ai.criticModel === 'anthropic/claude-sonnet-4.6'`
    - Importing `./src/ai/critic.js` yields `typeof judgeSpecificity === 'function'` AND `typeof critiqueDraft === 'function'`
    - `await judgeSpecificity('x')` returns an object with boolean `has_proper_nouns` and boolean `has_time_expressions`
    - `await critiqueDraft('x', {})` returns `null` (Wave 1 stub contract)
    - `package.json` `scripts.test` value contains the substring `src/ai/critic.test.js`
    - `package.json` `scripts.check` value contains the substring `src/ai/critic.js`
    - `package.json` `scripts['test:critic']` exists and equals `node --test src/ai/critic.test.js`
    - The string `anthropic/claude-sonnet-4.6` (dot, NOT hyphen) appears in `src/ai/critic.js` AND in `src/config.js`
    - No new dependencies were added to `package.json`
  </acceptance_criteria>
  <done>The skeleton, the config field, and the script wiring are committed. All four files compile/parse, the stubs are callable, and the contract for Wave 2 is locked. No OpenRouter call has been written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/ai/critic.test.js with all 6 RED test cases (integration tests)</name>
  <files>src/ai/critic.test.js</files>
  <read_first>
    - src/ai/metrics.test.js (lines 1-145 — full file; copy header comment style, fixture style, describe/it style, assert style)
    - src/ai/metrics.js (lines 247-270 — scoreDraft signature + return shape, used by Fixture 3 test)
    - src/ai/critic.js (Wave 1 stub created in Task 1 — confirms imports resolve)
    - .planning/phases/02-critic-integration/02-RESEARCH.md (Pattern 3 "Critique JSON Schema", Pattern 5 "METRICS-03 Specificity Judge", "Code Examples / Test Fixtures" lines 654-721)
    - .planning/phases/02-critic-integration/02-PATTERNS.md (section "src/ai/critic.test.js")
    - .planning/phases/02-critic-integration/02-VALIDATION.md (Per-Task Verification Map, "Important" guard block lines 27-32)
  </read_first>
  <behavior>
    - Test file loads cleanly under `node --check src/ai/critic.test.js`
    - When OPENROUTER_API_KEY is UNSET: file prints "[critic.test] OPENROUTER_API_KEY not set — skipping" and exits 0 BEFORE any test runs
    - When OPENROUTER_API_KEY is set AND stubs from Wave 1 are in place: tests run and FAIL (RED state) — confirming the contract is enforced
    - All 6 test cases listed below exist as `it(...)` blocks
    - Test 6 imports and calls `scoreDraft(CLEAN_DRAFT)` and asserts `skip_pipeline === true` BEFORE asserting `total >= 12` (validates the gate-correlation precondition)
    - GENERIC_DRAFT contains zero proper nouns (no personal names, no place names) and zero time expressions
    - SPECIFIC_DRAFT contains "Рома", "Митино", and "Шесть утра" verbatim
  </behavior>
  <action>
Create `src/ai/critic.test.js` with EXACTLY this content. The fixtures are taken verbatim from RESEARCH.md "Code Examples / Test Fixtures" (lines 654-721) and the existing `CLEAN_DRAFT` from `src/ai/metrics.test.js` (lines 11-35).

```js
// Интеграционные тесты для src/ai/critic.js. Запуск: node --test src/ai/critic.test.js
// Покрытие: PIPELINE-03 (5-dimension critique), METRICS-03 (specificity judge),
// MODELS-02 (critic model identity).
// Требует OPENROUTER_API_KEY в .env — реальные API вызовы.

// API key guard MUST appear before the imports that touch the critic module so we do not
// even attempt the integration tests in a CI/dev environment without credentials.
if (!process.env.OPENROUTER_API_KEY) {
  console.log('[critic.test] OPENROUTER_API_KEY not set — skipping');
  process.exit(0);
}

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { judgeSpecificity, critiqueDraft } from './critic.js';
import { scoreDraft } from './metrics.js';

// Fixture 1 (RESEARCH.md Pattern 5): zero proper nouns, zero time expressions.
// Expected: judgeSpecificity → {has_proper_nouns: false, has_time_expressions: false}
// Expected: critiqueDraft.story_specificity.score <= 1
const GENERIC_DRAFT = [
  '[Куплет 1]',
  'Он встаёт по утрам и идёт на работу,',
  'Она ждёт его дома с горячим чаем.',
  'Каждый день одно и то же — вот такая суббота,',
  'Так они живут, никуда не уезжая.',
  '[Припев]',
  'Ты мой герой, ты мой идеал,',
  'Ты моя звезда, ты мой свет.',
  'Без тебя бы я не встал,',
  'Дороже тебя в мире нет.',
  '[Куплет 2]',
  'Он сильный и добрый, всегда поддержит,',
  'Она красивая, умная, нежная.',
  'Вместе они пройдут любую нежность,',
  'Их любовь — бесконечная, безбрежная.',
  '[Бридж]',
  'Годы идут, но чувства крепнут,',
  'Счастье живёт в их маленьком доме.',
  'Они никогда не уснут в разлуке,',
  'Их сердца бьются в одном ритме.',
  '[Финал]',
  'Вот такая история любви,',
  'Простая, но честная — живи.',
].join('\n');

// Fixture 2 (RESEARCH.md Pattern 5): proper nouns (Рома, Митино) + time (Шесть утра).
// Expected: judgeSpecificity → {has_proper_nouns: true, has_time_expressions: true}
// Expected: critiqueDraft.story_specificity.score >= 2
const SPECIFIC_DRAFT = [
  '[Куплет 1]',
  'Шесть утра, а Рома уже шнурует кросс,',
  'Район Митино спит, фонарь горит — и холодно до слёз.',
  'Турник скрипнул, двадцать раз — и выдох в тишину,',
  'Пока соседи спят, считает Рома луну.',
  '[Припев]',
  'С днём рождения, Рома — в путь,',
  'Город спит, а ты — не свернуть.',
  'Впереди всех, и не догнать,',
  'Дорога знает — Рому ей встречать.',
  '[Куплет 2]',
  'В выходной — ружьё, рассвет, болото, грязь,',
  'Три часа в засаде, а потом домой летя.',
  'Мать кричит: «Опять сапожищи в прихожей!»',
  'Рома улыбнётся тихо — мам, ну что ты, Боже...',
  '[Бридж]',
  'Девчонки пишут, а Рома весь в подходе,',
  'Телефон молчит, пока штангу не уронит.',
  'Прочитает позже, усмехнётся — ну ок,',
  'И снова педали — ветер бьёт в висок.',
  '[Финал]',
  'Шесть утра, фонарь, турник — скрип знакомый,',
  'Ещё один рассвет встречает Рома.',
].join('\n');

// Fixture 3: a clean draft that passes the Phase 1 gate (skip_pipeline === true).
// Identical to CLEAN_DRAFT in src/ai/metrics.test.js — kept inline so this test file is
// self-contained and does not couple to metrics.test.js exports.
// Expected: scoreDraft(CLEAN_DRAFT).skip_pipeline === true
// Expected: critiqueDraft(CLEAN_DRAFT, scoreDraft(CLEAN_DRAFT)).total >= 12
const CLEAN_DRAFT = [
  '[Куплет 1]',
  'Шесть утра, а Рома уже шнурует кросс,',
  'Район спит, фонарь горит — и холодно до слёз.',
  'Турник скрипнул, двадцать раз — и выдох в тишину,',
  'Пока соседи спят, считает Рома луну.',
  '[Припев]',
  'С днём рождения, Рома — в путь,',
  'Город спит, а ты — не свернуть.',
  'Впереди всех, и не догнать,',
  'Дорога знает — Рому ей встречать.',
  '[Куплет 2]',
  'В выходной — ружьё, рассвет, болото, грязь,',
  'Три часа в засаде, а потом домой летя.',
  'Мать кричит про сапоги в прихожей нашей,',
  'Рома улыбнётся тихо — мам, ну что ты, Боже...',
  '[Бридж]',
  'Девчонки пишут, а Рома весь в подходе,',
  'Телефон молчит, пока штангу не уронит.',
  'Прочитает позже, усмехнётся — ну ок,',
  'И снова педали — ветер бьёт в висок.',
  '[Финал]',
  'Шесть утра, фонарь, турник — скрип знакомый,',
  'Ещё один рассвет встречает Рома.',
].join('\n');

const DIMS = [
  'story_specificity',
  'chorus_identity',
  'rhyme_quality',
  'singability',
  'emotional_honesty',
];

describe('judgeSpecificity (METRICS-03)', () => {
  it('returns false/false for generic draft with no proper nouns or time expressions', async () => {
    const result = await judgeSpecificity(GENERIC_DRAFT);
    assert.strictEqual(result.has_proper_nouns, false,
      'GENERIC_DRAFT contains no personal names or place names');
    assert.strictEqual(result.has_time_expressions, false,
      'GENERIC_DRAFT contains no specific time expressions');
  });

  it('returns true/true for specific draft with Рома, Митино, Шесть утра', async () => {
    const result = await judgeSpecificity(SPECIFIC_DRAFT);
    assert.strictEqual(result.has_proper_nouns, true,
      'SPECIFIC_DRAFT contains "Рома" and "Митино" as proper nouns');
    assert.strictEqual(result.has_time_expressions, true,
      'SPECIFIC_DRAFT contains "Шесть утра" as a time expression');
  });
});

describe('critiqueDraft (PIPELINE-03)', () => {
  it('returns valid JSON with all 5 dimensions present, each score in 0-3, plus total', async () => {
    const metrics = scoreDraft(GENERIC_DRAFT);
    const critique = await critiqueDraft(GENERIC_DRAFT, metrics);
    assert.ok(critique !== null, 'critiqueDraft must not return null on a well-formed draft');
    for (const dim of DIMS) {
      assert.ok(typeof critique[dim]?.score === 'number',
        `${dim}.score must be a number, got ${JSON.stringify(critique[dim])}`);
      assert.ok(critique[dim].score >= 0 && critique[dim].score <= 3,
        `${dim}.score must be 0-3, got ${critique[dim].score}`);
    }
    assert.ok(typeof critique.total === 'number',
      'total must be a number');
    assert.ok(critique.total >= 0 && critique.total <= 15,
      `total must be 0-15, got ${critique.total}`);
    // Re-computed total guard (RESEARCH.md Pitfall 2)
    const expectedTotal = DIMS.reduce((s, d) => s + critique[d].score, 0);
    assert.strictEqual(critique.total, expectedTotal,
      `total must equal sum of dimension scores: expected ${expectedTotal}, got ${critique.total}`);
  });

  it('failing dimension (score <= 1) has non-empty rewrite_instructions', async () => {
    const metrics = scoreDraft(GENERIC_DRAFT);
    const critique = await critiqueDraft(GENERIC_DRAFT, metrics);
    assert.ok(critique !== null, 'critique must not be null');
    const failing = DIMS.filter(d => critique[d].score <= 1);
    assert.ok(failing.length >= 1,
      `GENERIC_DRAFT should fail at least one dimension; got scores ${DIMS.map(d => `${d}=${critique[d].score}`).join(', ')}`);
    for (const dim of failing) {
      assert.ok(typeof critique[dim].rewrite_instructions === 'string'
        && critique[dim].rewrite_instructions.trim().length > 0,
        `${dim} has score ${critique[dim].score} but rewrite_instructions is empty`);
    }
  });

  it('keep_sections has at least 2 entries', async () => {
    const metrics = scoreDraft(SPECIFIC_DRAFT);
    const critique = await critiqueDraft(SPECIFIC_DRAFT, metrics);
    assert.ok(critique !== null, 'critique must not be null');
    assert.ok(Array.isArray(critique.keep_sections),
      'keep_sections must be an array');
    assert.ok(critique.keep_sections.length >= 2,
      `keep_sections must have >= 2 entries, got ${JSON.stringify(critique.keep_sections)}`);
  });

  it('Phase 1 passing draft (skip_pipeline=true) produces critique total >= 12', async () => {
    const metrics = scoreDraft(CLEAN_DRAFT);
    // Precondition: this fixture really does pass the Phase 1 gate.
    assert.strictEqual(metrics.skip_pipeline, true,
      `precondition: CLEAN_DRAFT must pass Phase 1 gate; metrics=${JSON.stringify(metrics)}`);
    const critique = await critiqueDraft(CLEAN_DRAFT, metrics);
    assert.ok(critique !== null, 'critique must not be null on a clean draft');
    assert.ok(critique.total >= 12,
      `Phase 1-passing draft must score total >= 12, got ${critique.total}`);
  });
});
```

DO NOT add any other test cases. DO NOT mock fetch. DO NOT add timeout overrides. The 6 cases above (2 in `judgeSpecificity` describe + 4 in `critiqueDraft` describe) are the complete contract.

Note: in the Wave 1 RED state, the `critiqueDraft` tests will fail because the stub returns null and the assertions `assert.ok(critique !== null)` reject that. The `judgeSpecificity` tests will fail because the stub returns false/false for both fixtures (passing test 1 by coincidence but failing test 2). This is the expected RED state.
  </action>
  <verify>
    <automated>node --check src/ai/critic.test.js && OPENROUTER_API_KEY="" node --test src/ai/critic.test.js 2>&1 | grep -q "OPENROUTER_API_KEY not set" && echo "OK guard works"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/ai/critic.test.js` exits 0
    - File line 1-3 contains a header comment naming the module + requirements (PIPELINE-03, METRICS-03, MODELS-02)
    - The API-key guard appears BEFORE any `import` that resolves `./critic.js` and exits via `process.exit(0)` when the env var is empty
    - Running with `OPENROUTER_API_KEY=""` exits 0 with stdout containing "OPENROUTER_API_KEY not set"
    - File contains exactly two `describe(...)` blocks: one for `judgeSpecificity (METRICS-03)`, one for `critiqueDraft (PIPELINE-03)`
    - File contains exactly six `it(...)` blocks total (2 + 4)
    - File contains the string `'Рома'` (used in SPECIFIC_DRAFT and CLEAN_DRAFT)
    - File contains the string `'Митино'` (SPECIFIC_DRAFT only)
    - File contains the string `'Шесть утра'` (verifying the time-expression fixture)
    - File contains the array literal `['story_specificity', 'chorus_identity', 'rhyme_quality', 'singability', 'emotional_honesty']` as the `DIMS` constant
    - The Fixture 3 test asserts `metrics.skip_pipeline === true` as a precondition before asserting `critique.total >= 12`
    - The "all 5 dimensions" test re-computes `expectedTotal` from dimension scores and asserts it matches `critique.total` (RESEARCH Pitfall 2)
    - When `OPENROUTER_API_KEY` is set and Wave 1 stubs are in place, the test run reports failures (RED state) — proving the contract is real
  </acceptance_criteria>
  <done>The test file is committed. With Wave 1 stubs, `OPENROUTER_API_KEY="" npm run test:critic` exits 0 (skip path); with the env var set, the suite runs and shows failing assertions (RED state). The contract for Wave 2 is fully specified by these 6 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local process → OpenRouter | Outbound HTTPS only; same boundary as the existing generator call in `src/ai/client.js`. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | `OPENROUTER_API_KEY` env var | accept | Same key as the generator already uses; no new credential. Loaded via `config.ai.apiKey` from `dotenv`; not logged anywhere in the new module. |
| T-02-02 | T | Critic JSON parsing | mitigate | Wave 2 task validates parsed shape (DIMS check, score range 0-3, keep_sections.length >= 2) and re-computes `total` from dimension scores. Wave 1 sets up the test contract that enforces these validations. |
| T-02-03 | D | Critic call latency causing user-visible stall | accept | Phase 3 owns the orchestration timeout (150s budget). Wave 1 only adds stubs — no live API call yet. Wave 2 limits to 2 attempts with 1500ms backoff. |
</threat_model>

<verification>
1. `npm run check` — must pass (now includes critic.js)
2. `OPENROUTER_API_KEY="" npm run test:critic` — must exit 0 with "skipping" message
3. `OPENROUTER_API_KEY=$ACTUAL_KEY npm run test:critic` — must produce failing test output (RED state confirms contract)
4. `node -e "import('./src/config.js').then(m => console.log(m.config.ai.criticModel))"` — must print `anthropic/claude-sonnet-4.6`
</verification>

<success_criteria>
- `src/ai/critic.js` exists with two exported async stubs
- `src/ai/critic.test.js` exists with 6 RED test cases and the API-key guard
- `config.ai.criticModel` defaults to `'anthropic/claude-sonnet-4.6'` (dot notation)
- `npm run check` passes
- `npm test` invokes both metrics + critic test files
- The model ID `anthropic/claude-sonnet-4.6` appears verbatim in `src/ai/critic.js` AND `src/config.js`
- No `reasoning` parameter, no `temperature` value, no `response_format` value is set in the critic.js stub (those belong to Wave 2 implementation)
- No new npm dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/02-critic-integration/02-01-SUMMARY.md` documenting:
- Files created/modified (with line counts)
- Confirmation that the RED state is reachable
- Confirmation of the API-key guard behavior
- Pointer to Wave 2 (Plan 02) for the implementation
</output>
